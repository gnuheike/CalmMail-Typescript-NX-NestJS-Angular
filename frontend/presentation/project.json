{
  "name": "frontend-presentation",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "frontend/presentation",
  "prefix": "lib",
  "projectType": "library",
  "tags": [],
  "implicitDependencies": [
    "frontend-application"
  ],
  "targets": {
    "build": {
      "executor": "@nx/angular:ng-packagr-lite",
      "outputs": [
        "{workspaceRoot}/dist/{projectRoot}"
      ],
      "options": {
        "project": "frontend/presentation/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "frontend/presentation/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "frontend/presentation/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production",
      "dependsOn": [
        "^build"
      ]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "frontend/presentation/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
