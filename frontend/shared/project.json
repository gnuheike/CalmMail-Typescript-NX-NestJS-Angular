{
  "name": "frontend/shared",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "frontend/shared",
  "projectType": "library",
  "tags": [
    "scope:shared",
    "type:infra"
  ],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/frontend/shared",
        "main": "frontend/shared/index.ts",
        "tsConfig": "frontend/shared/tsconfig.lib.json",
        "assets": [
          "frontend/shared/*.md"
        ],
        "format": [
          "cjs"
        ],
        "generatePackageJson": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "frontend/shared/jest.config.ts"
      }
    }
  }
}
